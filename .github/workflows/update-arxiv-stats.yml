name: Update stats

on:
  schedule:
    - cron: '0 2 * * 2-6'   # 02:00 UTC Tue–Sat (after arXiv publishes Mon–Fri papers)
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install firebase-admin

      - name: Run arXiv fetch
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: python scripts/fetch_arxiv.py

#      - name: Run energy prices fetch
#        env:
#          FIREBASE_SERVICE_ACCOUNT: #$#{{ secrets.FIREBASE_SERVICE_ACCOUNT }}
#          EIA_API_KEY: #$#{{ secrets.EIA_API_KEY }}
#        run: python scripts/fetch_energy.py